``` sql
SELECT
    PT_NAME,
    PT_NO,
    GEND_CD,
    AGE,
    IF(TLNO IS NOT NULL, TLNO, "NONE") AS TLNO -- IF(TLNO, TLNO, "NONE") 도 가능.
FROM PATIENT
WHERE GEND_CD = 'W' AND AGE <= 12
ORDER BY AGE DESC, PT_NAME ASC
```
